name: Price Tracker

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install requests

      - name: Run bot
        run: python bot.py

      - name: Commit changes
        run: |
          git config user.name "price-bot"
          git config user.email "bot@github.com"
          git add price.json
          git commit -m "price update" || echo "no changes"
          git push
